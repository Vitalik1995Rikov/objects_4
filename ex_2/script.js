let schedule = {};

function isEmpty(obj) {
    for (let key in obj) {
        return false;
    } return true;
}

schedule[name] = 'Alex';

console.log(isEmpty(schedule)); //работает, если цикл пошел, значит свойство есть, значит false